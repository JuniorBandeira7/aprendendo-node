<h1>Hello world!</h1>
{{#if approved}}
    <p>Cadastro aprovado</p>
{{else}}
    <p>cadastro precisa de aprovação</p>
{{/if}}
<p>o nome do usuario é {{user.name}} {{user.username}}</p>
{{#if auth}}
    <p>Voce esta autenticado</p>
    <a href="/dashboard">dashboard</a>
{{/if}}
